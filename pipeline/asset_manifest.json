{
  "_comment": "Every non-generated media asset the video pipeline is allowed to use, with its license. quality_check.py's copyright gate reads this file and fails the run if assemble_video.py (or any future stage) references something not listed here — so adding real B-roll or music later requires deliberately logging its license here first, rather than silently shipping unlicensed content.",
  "approved_tts_voices": [
    "en-US-GuyNeural",
    "en-US-AriaNeural",
    "en-US-ChristopherNeural",
    "en-US-JennyNeural",
    "en-US-EricNeural",
    "en-US-MichelleNeural"
  ],
  "_tts_note": "Microsoft Edge neural voices (via the free edge-tts library) are usable for this kind of content; they are not a cloned or impersonated real person's voice.",
  "approved_fonts": [
    {"name": "Poppins", "license": "SIL Open Font License 1.1", "source": "Google Fonts"},
    {"name": "DejaVu Sans", "license": "Bitstream Vera / public-domain-like license", "source": "system font, bundled with most Linux distros"}
  ],
  "broll_and_music_assets": [],
  "_broll_and_music_note": "Empty by design, and stays empty for the Pexels B-roll fetch_broll.py adds — that footage is fetched dynamically per video, not a fixed local asset, so it's licensed per-video via a broll_credits.json file quality_check.py reads instead of a static entry here. This list is for any FIXED asset you add by hand later (e.g. a licensed music track reused across every video) — add an entry for each: {\"file\": \"...\", \"source\": \"...\", \"license\": \"...\", \"url\": \"...\"}. quality_check.py fails closed if it finds referenced media that isn't logged either here or in a video's own broll_credits.json."
}
